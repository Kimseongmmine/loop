# LOOP — 프로젝트 기록 (CLAUDE.md)

지금 무엇이 참인지만 적는다. 무엇이 바뀌었는지는 `git log`에 있다.

## 무엇
목표와 내 상황을 적어두고 "계획 생성"을 누르면 AI가 하루(9~24시) 시간표와 식단을 짜주는 개인 대시보드 PWA.
밤(21시 이후)에 누르면 내일 것, 낮이면 오늘 것(`activeDate`). 생성은 온디맨드다 — 자동으로 돌지 않는다.
AI가 없거나 실패하면 고정 템플릿 시간표로 폴백한다. 앱은 안 깨진다.

## 왜 (사용자 맥락)
매일 "뭘 할지" 정하는 결정 자체가 실패 지점(관성 약함). 그 결정을 AI에게 넘긴다.
빽빽한 15시간 계획은 1일차에 포기 → **핵심 3개만 하면 성공**(무너짐 방지)이 핵심 장치.
강제성은 접속 스트릭만. 사람·돈·잔소리는 사용자가 전부 거부했다.
성공 지표는 완료가 아니라 **착수**다 — 미루기의 본질은 시작 지연이라는 진단에서 나왔다.

## 스택 (변경 금지)
- 바닐라 JS + HTML + CSS. 빌드도구·프레임워크·의존성 0
- 파일 4개: `index.html` / `app.js` / `style.css` / `manifest.json` (+ 저장소 문서, 아이콘 png)
- 저장 localStorage만. 서버 없음. **GitHub Pages 정적 배포** — https://kimseongmmine.github.io/loop/ · `git push`하면 자동 갱신
- **PC(데스크톱 브라우저)에서만 쓴다.** 터치 제스처를 구현할 필요가 없고, 드래그는 브라우저 기본 API로 충분하다
- AI 제공자 둘. **키가 있으면 Gemini를 먼저 쓴다**(무료 티어가 더 안정적) → 없으면 OpenRouter → 둘 다 없으면 템플릿
  - `aiChat`이 분기점. 키는 localStorage에만 두고 코드·깃에 절대 넣지 않는다
- `app.js` 끝의 `module.exports`는 node에서 `require`할 수 있게 유지한다. 순수 함수를 추가하면 여기 export한다

## 개발 규약
- 스택·파일 4개 유지. 스펙 밖 기능은 사용자 승인 후에만
- 시각과 네트워크는 주입·스텁으로 다룬다. 실제 시각이나 네트워크를 기다리는 코드는 넣지 않는다
- v0("하루 한 줄", 밤/아침 타이머)는 폐기됐다. git 히스토리에만 존재

## 데이터 모델 (localStorage)

| 키 | 내용 |
|---|---|
| `loop.profile` | `{ fixed, rhythm, traits, prefs, places, goals:[…] }` |
| `loop.plans` | `{ "YYYY-MM-DD": { blocks:[…], generatedAt, source:"template"\|"ai" } }` |
| `loop.visits` | `["YYYY-MM-DD"]` → 스트릭(오늘부터 역순 연속) + 접속 점그리드 |
| `loop.events` | `{ "YYYY-MM-DD": "14:00 병원 / 19시-21시 알바" }` — 그날 하루만의 예외 일정 |
| `loop.energy` | `{ "YYYY-MM-DD": "high"\|"mid"\|"low" }` — 배터리 |
| `loop.notes` | `{ "YYYY-MM-DD": "한 줄 회고" }` |
| `loop.done` | `[{ text, goalTitle, date }]` — "내가 끝낸 것들" 영구 보관 |
| `loop.backup_at` | 마지막 백업일 |
| `loop.notify` · `loop.or_key` · `loop.or_model` · `loop.gemini_key` · `loop.gemini_model` | 설정. **키 둘은 백업에서 제외** |

**goal** = `{ id, title, deadline, tasks:[{id,text,done}], analyzedAt? }` · 진행도 = done/total (`goalProgress`)

**block** = `{ id, time, text, place, first, goalId, taskId, core, started, startedAt, onTime, done, event?, move? }`
- 학습 블록만 `goalId`+`taskId`를 갖는다. 체크하면 해당 task가 완료되어 진행도에 연동된다. 생활 블록은 `null`
- 구버전 계획엔 `place`·`first`가 없다. 없으면 표시하지 않는다 — 마이그레이션 불필요

프로필 5필드는 전부 선택이다: `fixed`(고정일정) `rhythm`(하루리듬) `traits`(특성) `prefs`(선호) `places`(자주 가는 장소·이동시간).
`loadProfile`이 구버전 `situation` → `traits`로 옮기고, `places`가 `undefined`일 때만 기본값을 시드한다(빈 입력창 금지 원칙). 빈 문자열로 지우면 지운 대로 둔다.

## 화면 구조 (탭 · 해시 라우팅)
`TABS` = 오늘 📋 / 목표 🎯 / 흐름 🀄 / 설정 ⚙. 하단 고정 탭바(`.tabbar`), `#/goal` 처럼 해시로 이동해서 뒤로가기와 딥링크가 된다.
- `currentTab()`은 `location.hash` 우선, 없으면 모듈 변수 `tabKey`(node 환경엔 `location`이 없다). `goTab(k)`가 해시·렌더·스크롤 리셋을 함께 한다
- **오늘**: 지금/다음 줄 → 오늘 계획 → 식단 → 다시 짜기 도구 → 오늘 한 줄 → 명언
  계획이 화면 맨 위에 온다. 배터리·특별 일정·생성 버튼은 계획 **아래** — 만들고 나면 매일 볼 필요가 없다
- **목표**: 진행도 + 목표·과제 / **흐름**: 오늘의 운세 + 기록·월운·세운·대운 + 원국 / **설정**: 설정 + 기록 + 할 수 있는 것
- 접속 스트릭은 상단바(`.tstreak`). 설정 탭에 묻히면 안 된다
- 렌더 함수는 목적별로 쪼개져 있다: `renderNowbar` / `renderPlanTools` / `renderGuide` / `renderQuote` / `renderGoalsPanel` / `renderFocus`
- 탭 안 이동은 `scrollTo(id)`. 섹션 id: `today` `prog` `meals` `note` `flow` `fortune` `settings`

### 팔레트 (水金)
정본은 `style.css` 맨 위 주석과 `:root`다. 여기선 규칙만: **사용자 원국의 용신 水 · 희신 金을 따라 빨강·주황·앰버·크림이 이 파일에 존재하지 않는다.**
라이트 = 金(은회백), 다크 = 水(심해). 다크가 실질 기본이다 — 21시 이후가 주 사용 시간이므로.
실패는 무채색과 굵기로 알린다(`--danger`도 붉지 않다). 색을 추가할 땐 대비비를 계산해서 넣는다(텍스트 4.5:1, UI 3:1).
`index.html`의 `theme-color`도 라이트·다크 두 줄로 맞춰 둔다.

**돌아오는 길은 셋이다.** 탭바 하나에만 의존하면 그게 깨졌을 때 갇힌다.
로고가 홈 버튼(`.mark.homebtn`) · 오늘 탭이 아닐 때만 나타나는 `← 오늘` · 실행 모드의 `오늘 계획` 버튼(실행 모드는 일부러 탭바를 숨긴다).

### 기능이 드러나게
- 히어로 아래 **"할 수 있는 것"** 목록(`.guide`) 5개: 실행 모드 · 목표 쪼개기 · 식단·장보기 · 오늘의 운세 · 백업. 각 줄 = 아이콘 + 이름 + 뭘 하는지 한 줄 + 바로 실행
  - 조건이 안 되면 잠그고 이유를 그 자리에 쓴다. `mealsAvailable()`이 식단 카드가 뜰 조건을 미리 계산해 죽은 버튼을 없앤다
- 모든 카드 h2 아래에 `.what` 한 줄 설명

## 하루 계획

### 생성 (`generatePlan`)
`boot()`은 방문 기록 → `render()`만 한다. 계획은 사용자가 버튼을 눌러야 생긴다.
1. AI가 있으면 과제 없는 목표를 먼저 분해한다(`aiBreakdownGoal`)
2. `nextPendingTasks(profile, 6)`으로 후보를 뽑아 `aiGeneratePlan` → 성공하면 `fillPlaces`만 태운다(이동 블록은 AI가 직접 넣는다)
3. 실패하면 템플릿 경로: `templatePlan` → `mergeEventBlocks` → `fillPlaces` → `insertCommutes`
4. `opts.keep`이 있으면 지난/완료 블록을 보존하고, 겹치는 새 블록은 버린다. 마지막에 항상 시간순 정렬
- `generating` / `breaking` 로딩 플래그가 켜져 있으면 자동 리렌더를 멈춘다

### AI 컨텍스트
`profileContext(profile)`가 5필드 + 마감을 조립해 프롬프트에 주입한다. 규칙: 고정일정 시간대엔 학습블록 금지, 하루리듬 기상~취침 안, 집중시간에 핵심 배치.
목표 쪼개기는 JSON 대신 **번호/불릿 목록**으로 받아 `parseTaskList`로 파싱한다(작은 무료모델이 목록은 잘 만든다). JSON도 여전히 허용. 프리앰블·콜론줄은 버린다.
과제는 구체적이어야 한다 — '공부하기' 금지, '3장 1-10번 풀기' 요구.

### 특별 일정
`parseEvents`는 `:` 또는 `시`가 붙은 것만 시각으로 인정한다 → "3장 1-10번 풀기"를 시간으로 오해하지 않는다. "9시 30분"·"오후 2시"를 정규화하고, 시각이 없으면 `time:null`.
`mergeEventBlocks`가 템플릿 폴백에도 일정을 실제 블록으로 넣고 겹치는 템플릿 블록을 비운다 → AI가 죽어도 일정이 사라지지 않는다.
적어뒀는데 현재 계획에 없으면 "다시 생성하면 반영됩니다"라는 사실만 고지한다.

### 장소와 이동
`placeRules(profile)`가 자유 텍스트에서 도서관/카페/운동/집 이름과 이동 시간을 뽑는다(`(\d+)분`, 기본 20·최대 120). 반환에 `mins`(장소→분)와 `night`(집 근처 학습 장소, `/집.*(카페|독서실|도서관)/`, 없으면 `home`)가 있다.
`commuteBetween(a, b, rules)` = 두 장소의 분 중 큰 쪽. 모르면 기본값, 같은 장소면 0.

`fillPlaces`는 `place`가 비어 있는 블록만 채운다. `event`와 이미 있는 `place`는 건드리지 않는다.
- 학습 180분 이상 = 카페 / 미만 = 도서관, **18시(`EVENING_MIN`) 이후 학습 = `night`**
- 운동 = 운동 장소, 식사·휴식 = 직전 장소(단 운동 뒤엔 집), 21시 이후 = 집
- "21시 이후는 집"은 `taskId`/`core`가 있는 진짜 과제 블록에만 양보한다(밤 마무리 독서는 집으로 돌아온다)

`insertCommutes(blocks, rulesOrMin)` — 장소가 바뀌면 이동 블록을 넣는다. 숫자를 주면 고정 시간, `placeRules` 결과를 주면 장소별 시간.
**이동은 앞의 짧은 휴식을 흡수한다.** 직전이 짧은 생활 블록(`isFillerBlock`, 길이 ≤ 이동 시간)이면 그 자리를 이동이 대신한다 — 걷는 게 곧 휴식이고 행도 하나 준다. 모자란 만큼만 뒤를 미룬다.
- 뒤로 미루면 목적지 활동이 깎이고(운동 60→35분), 앞에서 빌리면 앞이 깎인다(운동 20분). 둘 다 틀렸다
- `DEFAULT_PLACES`는 장소별 분을 적는다(`경북대 중앙도서관 25분 / 집 앞 스터디카페 5분 / 집 책상 / 수영장 15분 …`). 저장된 값이 **옛 기본값 문자열과 정확히 같을 때만**(`OLD_DEFAULT_PLACES`) 새 기본값으로 올린다

### 표시: 지금 구간만 펼치고, 자잘한 건 얇게
계획표가 15행이면 폰에서 지금 할 일이 스크롤 아래로 내려간다.
- `isMicroBlock(b)` — `core`·`taskId`·`event`가 아니고 (이동 또는 25분 이하). 체크박스 없이 `.microrow` 한 줄로 그린다. 착수/완료 집계에 원래 안 들어가므로 잃는 게 없다
- `splitDay(blocks, nowMin, isToday, ahead=3)` → `{past, live, later}`. `live`는 진행 중 블록부터 4개. 내일 계획이면 `past` 없이 앞 4개
- `past`/`later`는 `markOpen`으로 접는다(펼친 상태는 리렌더에도 유지). 요약에 개수·구간·착수 집계

**결과: 첫 화면 4행 + 접힘 요약 2줄.** 하루가 14행이어도 스크롤 없이 지금 할 일이 보인다.

### 고치기 (재배치 엔진)
AI 계획이 틀렸을 때 통째로 다시 만드는 것 말고 방법이 없었다. 블록 하나가 틀려도 하루가 새로 나왔다.

원칙 1("결정을 사용자에게 되돌려주지 않는다")과의 타협:
- 기본값은 그대로 AI가 정한다. **고치기 모드에 들어가기 전엔 편집 UI가 존재하지 않는다**
- 여분 블록을 미리 만들어 고르게 하지 않는다. `⇄`를 눌렀을 때만 서랍이 열린다 → 평소에 결정이 늘지 않는다
- 근거: 자기가 짠 계획은 실제 수행량의 약 3배로 과욕(사전등록 RCT). 편집은 가능하되 눈에 안 띄는 곳에 둔다

편집 방식이 뭐든 한 경로로 모인다: `편집 → 이동 블록 제거 → 장소 비우기 → fillPlaces → insertCommutes → 저장`
- `editableBlocks` — `!isMicroBlock && !event`. 이동·짧은 휴식은 파생물이고 특별 일정은 시각 고정이다
- **`swapSlots(blocks, idA, idB)` — 시각은 자리에 남고 내용만 맞바꾼다.** `CARRIED = [text, goalId, taskId, first, core]`만 옮기고 `time`·`done`·`started`·`startedAt`·`onTime`은 안 따라간다. 시각을 안 건드리므로 특별 일정과 지나간 블록이 밀려나지 않고 하루 뼈대가 AI가 정한 대로 유지된다
- `shiftBlock(blocks, id, ±1)` — 옮길 수 있는 블록끼리만 세서 한 칸(micro는 건너뛴다). 끝이면 원본 그대로
- `swapTask` — 과제만 갈아끼우고 `first`는 지운다(`firstStep`이 다시 만든다)
- `dropBlock` — 빼고 앞 블록을 늘려 구멍을 안 남긴다. 첫 블록이면 뒤가 당겨진다
- `swapCandidates(profile, blocks, 4)` — 오늘 안 쓴 미완료 과제. **AI 호출 없음**
- `applyEdit(date, mutate)` — 유일한 저장 경로. 바뀐 게 없으면 `null`을 돌려주고 저장도 안 한다
  - **장소를 비우고 다시 계산하는 게 핵심**: 11시 학습을 19시로 옮기면 도서관 → 집 앞 스터디카페가 되고 이동 블록도 그에 맞춰 다시 생긴다

화면: 계획 카드 머리에 `✎ 고치기` / `완료` 토글(`editing`). 꺼져 있으면 이전과 100% 동일.
켜면 행마다 `≡` 손잡이 + `↑ ↓ ⇄ ✕`, 시작 버튼·배지는 숨김, `이따` 덩이를 강제로 펼침(볼 수 없는 건 못 옮긴다), 지난 것은 접힌 채(이미 끝난 건 못 고친다). 특별 일정·완료 블록은 손잡이 없이 `고정` 배지.

## 착수 · 첫 동작 · 실행 모드
근거: 시간·장소만 정하는 계획은 반복 행동에 효과가 없다(사전등록 RCT null, 출판편향 보정 d=.15). 살아남는 형태는 **접합점 큐**("A 끝나면 바로 B", d=.50)와 **구체적 첫 몸동작**이다.

### 착수(started)를 완료(done)에서 분리
- `onTime`은 **착수 시점에 한 번만** 굳는다. 두 번 눌러도 안 바뀐다
- `setBlockStarted(date, id, now)`는 멱등. `setBlockDone(true)`는 착수 기록이 없으면 그 순간을 착수로 본다(구버전 데이터와 같은 뜻)
- `coreStatus` → `{ done: 정시 착수 수, late, fin: 완료 수, total }`. 화면 문구는 **"착수 n/m · 완료 k"**
- `daySummary` / `recentStats`도 `b.onTime` 기준으로 통일. 구버전 계획은 done 시점에 onTime이 찍혔으므로 그대로 호환된다

### 첫 동작 (`firstStep`)
시작이 막히는 지점은 장소가 아니라 **다음 몸동작이 비어 있는 것**이다. 블록마다 5분 안에 끝나는 물리적 동작 한 줄을 둔다.
AI가 `first`를 채우고, 없으면 규칙으로 만든다: 이동 → "지금 일어나서 나가기" / 운동 → "옷 갈아입고 가방 챙기기" / 학습 → "자리에 앉아 {과제 26자} · 5분만" / 휴식·식사·특별일정 → 없음.

### 실행 모드 (`renderFocus`)
`focusId`가 있으면 `render()`가 **그 블록 하나만** 그린다. 히어로·계획·흐름·탭바 전부 안 그린다.
구성: 시각·장소 / 블록 제목 / **첫 동작(가장 크게)** / 남은 시간 바 / `[5분만 시작] [시작 기록] [완료]` / "끝나면 다음 · …".
5분 타이머는 `focusUntil`(ms) + 1초 인터벌. 끝나도 아무 평가도 하지 않는다.
진입: 히어로의 "지금/다음" 줄 전체, 계획 카드의 `▶ 실행 모드`, 블록 행의 `시작`.

## 목표와 진행도
목표 카드에 `🧩 AI로 과제 쪼개기`(`breakdownGoalNow`, `breaking` 로딩 플래그).
과제 완료율이 목표별 진행도(%)가 되고, 완료한 과제는 `loop.done`에 영구 보관된다.
`stalledTask`가 멈춘 과제를 짚는다.

## 사주 (흐름 탭)
전부 계산으로 나온다. 하드코딩된 결론은 없다.

- `julianDay(dateString)` — 그레고리력 → 율리우스 적일. `dayPillar` = `((julianDay + 49) % 60 + 60) % 60` → `STEMS[i%10] + BRANCHES[i%12]`
  - 기준점 두 개로 교차 검증한다: **1900-01-01 = 甲戌, 2000-01-01 = 戊午**
- `solarMonth`(절기 근사 `TERM_DAY`) → `monthPillar`(월지=절기월, 월간=오호둔) → `monthFlow(3개월)`
- `hourPillar(date, hour)` — 오자둔: `base = ((일간 index % 5) * 2) % 10`, 시간 = `(base + 시지 index) % 10`. 시지는 23시부터 두 시간씩(`(h+25)%24/2`)
- `gzTone`이 세운·월운·일진 판정을 공유한다(금수 = 순풍/전환, 화토 = 축적)
- `dayFortune(date)` → `{ gz, el, tone, act, hours }`. `hours`는 용신 水 · 희신 金 시간대(15–17 申, 17–19 酉, 21–23 亥) 중 그날 오행과 겹치는 것을 강조

**원국** (생년월일 2002-07-05, 10시): `natalChart()` → **壬午년 丙午월 甲戌일 己巳시**. 일간 甲(목).
오행 개수 **목1 화4 토2 금0 수1** — 화토 6, 금 0. 용신 水 · 희신 金 · 기신 火土가 여기서 나온다.
`renderNatal()`이 네 기둥(일주 강조)과 오행 칩을 보여주고 한 줄로 결론만 말한다.

**`act`는 지시문만 쓴다. 그 날을 나쁘다고 하지 않는다.** 순풍·전환·축적·보통 넷 다 "오늘 뭘 하라"만 말한다.
- 근거: 부정적 운세는 인지 수행을 실제로 떨어뜨린다(실험 3개). 긍정 프레이밍은 올린다. "앱은 사용자를 평가하지 않는다"는 원칙과도 같은 방향

화면 고지: 절입일은 평년 근사값이라 실제와 하루 차이가 날 수 있다 · 출생 시각(巳시 = 원국에 화가 하나 더) · 일진은 달력 기준이고 사주 원칙으로는 23시부터 다음 날.

## 데이터 백업
localStorage는 지워진다. 사파리는 7일 미접속 시 스크립트 저장 데이터를 삭제하고, 용량이 부족하면 오리진을 통째로 지운다. 상한은 5MiB. 유실이 실재해서 넣었다.
- `DATA_KEYS` = profile / plans / visits / energy / notes / done / events. **API 키는 절대 내보내지 않는다**
- `exportPayload()` → `{ app:"loop", version:1, exportedAt, data:{키:원문자열} }`
- `applyImport(payload)` → 형식 확인 후 파싱되는 항목만 복구하고 개수를 반환한다
- 내보내기: 크로미움 PC는 `showSaveFilePicker`로 같은 파일 덮어쓰기, 그 외 Blob 다운로드. 가져오기 전 `confirm`
- `askPersist()`를 `boot`에서 호출한다(`navigator.storage.persist()`)
- 설정 하단 "데이터" 블록에 버튼·마지막 백업일·경고 문구

## 안 넣기로 한 것 (리서치 근거)
벌칙형 커밋먼트(55% 실패) · 알림 강화(앱 이탈 1위 원인) · 버디·소셜(자동 코치와 차이 없음이 RCT로 확인) · 21일 챌린지(자동화까지 18~254일, 정해진 기간 없음).

## 함정 (다시 밟지 말 것)
- **미디어 쿼리는 자기가 덮으려는 기본 규칙 뒤에 놓는다.** 데스크톱 `.tabbar` 규칙이 기본 규칙보다 앞(75행 vs 445행)에 있어서, 특이도가 같은 뒤쪽 `left:0; right:0`이 이기고 앞쪽 `transform: translateX(-50%)`만 살아남았다 → 900px 이상에서 탭바가 화면 왼쪽 밖으로 절반 사라졌다. 고친 규칙은 자기완결적으로: `left:50%; right:auto; transform:translateX(-50%); width:760px`
- **`profile.goals.forEach(function (g) {`는 진행도에도 있다.** 설정에서 목표를 떼어낼 때 이 줄을 그냥 찾으면 엉뚱한 범위를 자른다(실제로 한 번 그랬다). 함수 시작 인덱스부터 찾을 것
- **`insertCommutes`가 특별 일정을 밀지 않게** 뒤로 미루는 분기에 `!b.event` 조건이 있다. 병원 14:00이 14:25로 밀린 적이 있다. 흡수 분기는 뒤 블록을 안 건드리므로 그대로 둔다
- **블록 행이 `<label>`이라 안쪽 버튼 클릭이 체크박스로 샌다.** 행 안의 버튼은 전부 `e.preventDefault()`
- **드래그 중엔 60초 자동 리렌더를 멈춘다**(`!dragId` 조건). 안 그러면 끄는 도중 DOM이 날아간다
- **"1984-02-02 = 甲子"는 연주 이야기다.** 일주로는 丙寅. 혼동 금지
- **접힌 `<details>` 안의 행을 첫 화면으로 세지 않는다.** `.blocks`의 직계 자식만 첫 화면이다

## 검증 (자동 테스트 없음)
자동 테스트는 저장소에 없다. `app.js`의 `module.exports`가 node에서 붙일 수 있게 열려 있을 뿐이다.
손으로 고칠 때 최소한 이것들을 확인한다:
- **계획**: 장소별 이동 시간 · 옛 기본값 마이그레이션 · 18시 이후 저녁 규칙 · 흡수/폴백 · `isMicroBlock` 판정 · `splitDay` 경계(아침/오후/하루 끝/내일) · 첫 화면 행 수
- **고치기**: 시각 보존 · 기록 비이동 · micro 건너뛰기 · 구멍 없음 · `applyEdit`의 장소·이동 재계산 · 특별 일정 시각 고정 · 평소엔 손잡이 0개
- **이동**: 네 탭 전부에서 탭바·활성 표시 · 로고/`← 오늘`/실행 모드에서 한 번에 복귀 · CSS 데스크톱 규칙이 기본 규칙보다 뒤에 있는지
- **운세**: 두 기준점(1900-01-01 甲戌 / 2000-01-01 戊午) · `act`에 부정 단어(나쁜·불리·조심·실패…)가 없는지
