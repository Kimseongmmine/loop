# Product

<!-- impeccable:product-schema 1 -->

## Platform

web

## Users

**주 사용자: 단 한 명 — 제작자 본인(대학 3학년).** 실사용자가 늘어날 계획은 현재 없음.

상황과 할 일:
- 매일 "오늘 뭘 할지" 스스로 정하는 것이 실패 지점. 결정 자체가 부담이라 아무것도 시작하지 못하고 하루가 지나감
- 고교까지는 외부에서 짜준 틀(과외 스케줄) 안에서 최고 성과를 냈고, 대학에서 그 틀이 사라지자 성적이 무너짐 → 능력 문제가 아니라 **외부 구조의 부재**가 문제
- 만성적 미루기, 쉽게 지침, 몰아치고 뻗는 패턴. 한 번 어긋난 날은 하루 전체를 포기하는 경향
- **주 사용 기기는 PC(책상)**. 폰은 보조

**부차 청중: 이 링크를 보는 사람(채용·면접·동료).** 앱은 포트폴리오 결과물로 공개되어 있으므로, 처음 여는 사람이 설명 없이도 "무엇을 하는 물건인지" 알아볼 수 있어야 함. 다만 그들은 가입하거나 사용하지 않음.

## Product Purpose

하루 계획을 세우는 결정 부담을 AI에게 넘겨, 사용자가 **선택하지 않고 실행만** 하게 만드는 개인용 실행 도구.

저장된 개인 맥락(고정 일정·하루 리듬·특성·선호·목표)을 근거로, 버튼 한 번에 그날의 시간대별 계획과 식단을 생성한다. 사용자는 계획을 짜지 않고, 정해진 것을 제때 실행했는지만 기록한다.

성공은 "완벽한 하루"가 아니라 **무너지지 않고 다음 날 다시 여는 것**이다.

## Positioning

일반 습관·할일 앱은 사용자가 직접 입력하고 직접 열어야 작동한다. 이 사용자에게는 그 두 가지가 모두 실패 지점이었다(마이루틴·노션·투두리스트 전부 이탈).

LOOP의 차별 지점:
- **빈 입력창을 만들지 않는다.** 항상 무언가가 이미 채워져 있고, 사용자는 승인·실행만 한다
- **정시 판정.** 완료 여부가 아니라 "블록 시작 시각 ±5분 안에 눌렀는가"를 잰다. 미루기의 본질은 시작 지연이라는 진단에서 나온 지표
- **하루를 통째로 버리지 않게 한다.** 어긋난 시점부터 남은 시간만으로 재생성하는 경로가 있음
- **자기 데이터가 다음 계획에 되먹여진다.** 한 줄 회고·배터리 상태·미완료 항목이 다음 날 생성 입력으로 들어감

## Operating Context

- 하루 1~2회 접속. 밤(21시 이후)에는 다음 날 계획을, 낮에는 당일 계획을 생성한다
- 계획은 09:00~24:00 범위의 시간 블록. 고정 일정(수업·알바)은 요일별로 다르며 학습 블록으로 덮으면 안 된다
- 실행 중 체크는 블록 시작 시각 근처에서 발생한다. 늦은 체크도 기록되지만 "정시"로는 세지 않는다
- 무료 AI 모델은 응답 형식이 불안정하고 모델 목록도 수시로 바뀐다. 실패는 예외가 아니라 상시 조건이다

## Capabilities and Constraints

확정된 기능:
- 개인 프로필 저장(고정 일정 / 하루 리듬 / 나의 특성 / 선호·비선호), 목표와 마감일
- 목표를 구체적 실행 과제로 AI 분해 → 과제 완료율이 목표별 진행도(%)가 됨
- 일자별 시간표 + 식단 생성, 핵심 블록 표시, 정시/늦음 판정
- 지금·다음 블록 표시, 정시 알림(탭이 열려 있는 동안), 지금부터 재생성, 자동 밤 결산, 한 줄 회고
- 완료 과제 영구 보관("내가 끝낸 것들"), 접속 스트릭(부차 정보로 하단 배치)

기술 제약:
- 빌드 도구·프레임워크·의존성 없음. 저장은 브라우저 localStorage뿐 → **기기 간 동기화·백업 없음**
- 서버 없음. GitHub Pages 정적 배포
- 알림은 탭이 열려 있을 때만 동작(서비스 워커 미도입)

명시적으로 미확정:
- 다른 사용자 지원(계정·서버)은 현재 계획 없음. 다만 영구 배제로 확정된 것도 아님
- "핵심 3개" 개수는 현재 설계이며 불변 사양으로 확정되지는 않음

## Brand Commitments

- 이름: **LOOP**. 아이콘은 흰 링(1색), 파비콘/PWA 아이콘으로 사용 중
- **목소리(구속력 있음): 잔소리·응원·격려 금지.** 듣기 좋은 말, 과한 위로, 자책을 유도하는 문구 전부 배제. 건조한 사실과 숫자로 말한다. 실패했을 때도 앱은 나무라지 않고 기록만 남긴다
- 실패 표시는 회색(중립)으로, 성공은 조용한 초록으로. 실패를 붉게 경고하지 않는다

## Evidence on Hand

- 라이브: https://kimseongmmine.github.io/loop/ · 코드: https://github.com/Kimseongmmine/loop
- 실제 사용 데이터는 아직 없음(도구가 방금 완성됨). **사용자 수·성과·후기 등 어떤 성과 주장도 만들어내면 안 됨**
- 자산: `icon-192.png`, `icon-512.png`(흰 링). 그 외 이미지·사진·로고 자산 없음
- 기록 문서: `CLAUDE.md`(구현 결정), `README.md`

## Product Principles

1. **결정을 사용자에게 되돌려주지 않는다.** 빈 칸, 선택지 나열, "무엇을 하시겠습니까"는 실패다. 항상 기본값이 채워져 있어야 한다
2. **무너짐에 대비해 설계한다.** 한 번 어긋난 날에도 복구 경로가 보여야 한다. 완벽한 하루를 전제한 설계는 1일차에 버려진다
3. **기록은 정직해야 한다.** 하지 않은 것을 한 것처럼 세지 않는다. 대신 그 사실로 사용자를 비난하지도 않는다
4. **AI 실패는 상시 조건이다.** 어떤 AI 경로도 죽을 수 있고, 죽어도 앱은 계속 작동해야 한다(템플릿 폴백)
5. **비용은 0원을 유지한다(구속력 있음).** 유료 구독을 강제하지 않으며, 키가 없어도 기본 동작은 보장한다

## Accessibility & Inclusion

제품 고유의 접근성 요구사항으로 확정된 것은 없음. 다만 사용자가 눈 건조·피로를 겪어 화면 가독성(충분한 대비, 작지 않은 본문)과 회복 시간(수분·눈 휴식 블록)이 제품 안에 반영되어 있음.
